<!DOCTYPE html>
<html>

<head>
    <title>{{title}}</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <!-- Đường dẫn đến file CSS của Bootstrap -->
    <link rel='stylesheet' href='/stylesheets/login.css' />
    <!-- Đường dẫn đến file CSS của bạn nếu có -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <script src="/socket.io/socket.io.js"></script>
</head>

<body>
    <div class="container">
        <div aria-live="polite" aria-atomic="true" style="position: relative;">
            <div class="toast-container" style="position: absolute; top: 20px; right: 20px;">
                <div id="errorToast" class="toast" role="alert" aria-live="assertive" aria-atomic="true">
                    <div class="toast-header">
                        <strong class="mr-auto">Message</strong>
                        <button type="button" class="ml-2 mb-1 close" data-dismiss="toast" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                        </button>
                    </div>
                    <div class="toast-body" id="toastBody">
                    </div>
                </div>
            </div>
        </div>
        <div class="wrapper bg-white">
            <div class="h2 text-center">CampusPoly</div>
            <div class="h4 text-muted text-center pt-2">Đăng nhập vào hệ thống</div>
            <div class="text text-muted text-center">MS - CampusPoly</div>
            <form class="pt-3" id="loginForm" action="/login" method="POST">
                <div class="form-group py-2">
                    <div class="input-field">
                        <span class="far fa-user p-2"><i class="bi bi-person"></i></span>
                        <input type="text" placeholder="Username or Email Address" required class="" id="email"
                            name="email">
                    </div>
                </div>
                <div class="form-group py-1 pb-2">
                    <div class="input-field">
                        <span class="fas fa-lock p-2"><i class="bi bi-lock"></i></span>
                        <input type="password" placeholder="Enter your Password" required class="" id="password"
                            name="password">
                        <span class="far fa-eye-slash pr-2" id="togglePassword"><i class="bi bi-eye-slash"
                                id="toggleIcon"></i></span>
                    </div>
                </div>
                <button type="submit" class="btn btn-block text-center my-3" id="buttonLogin">
                    <span id="spinner" class="spinner-border spinner-border-sm" style="display:none;" role="status"
                        aria-hidden="true"></span>
                    <span id="buttonText">Log in</span></button>
                <span id="errorMessage"></span>
            </form>
        </div>
        <div class="version text text-center text-muted">MS CampusPoly - Phiên bản 1.0.0</div>
    </div>

    <script src="/javascripts/login.js"></script> <!-- Đường dẫn đến file JavaScript của bạn -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <!-- jQuery (cần thiết cho Bootstrap JavaScript plugins) -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
    {{!-- <script>
        $(document).ready(function () {
            $('#loginForm').on('submit', function (event) {
                event.preventDefault(); // Prevent default form submission

                // Get form data
                var formData = {
                    email: $('#email').val(),
                    password: $('#password').val()
                };

                // Disable the submit button, show loading indicator, and change button text
                $('#buttonLogin').prop('disabled', true);
                $('#spinner').show(); // Show spinner
                $('#buttonText').hide(); // Change button text to indicate processing

                // Send the form data to the server via AJAX
                $.ajax({
                    url: '/login', // URL of your login route
                    type: 'POST',
                    data: formData,
                    success: function (response) {
                        // Set a minimum display time for the spinner
                        setTimeout(function () {
                            // Re-enable the submit button and hide loading indicator
                            $('#buttonLogin').prop('disabled', false);
                            $('#spinner').hide(); // Hide spinner
                            $('#buttonText').show()
                            if (response.success) {
                                // If login is successful, redirect to homepage
                                window.location.href = '/';
                            } else {
                                // If login fails, display the error message in the toast
                                $('#errorMessage').text(response.message);
                            }
                        }, 1000); // Minimum display time of 1000 ms (1 second)
                    },
                    error: function () {
                        // Set a minimum display time for the spinner
                        setTimeout(function () {
                            // Re-enable the submit button and hide loading indicator
                            $('#buttonLogin').prop('disabled', false);
                            $('#spinner').hide(); // Hide spinner
                            $('#buttonText').show()

                            // Handle any unexpected errors from the server
                            $('#toastBody').text('An unexpected error occurred. Please try again.');
                            $('#errorToast').toast({
                                delay: 3000 // Show for 3 seconds
                            });
                            $('#errorToast').toast('show');
                        }, 1000); // Minimum display time of 1000 ms (1 second)
                    }
                });
            });
        });

    </script> --}}
</body>

</html>